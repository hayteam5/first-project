import { NgForOfDirective } from './ng-for-of.directive';

describe('NgForOfDirective', () => {
  it('should create an instance', () => {
    const directive = new NgForOfDirective();
    expect(directive).toBeTruthy();
  });
});
